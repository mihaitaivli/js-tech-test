{
  "name": "js-tech-test",
  "version": "1.0.0",
  "description": "Simple betting app",
  "main": "index.js",
  "scripts": {
    "test": "test",
    "start-docker": "docker-compose up -d",
    "build-app": "cd client && npm i && npm run build && cd ..",
    "serve-app": "cd client/build && npx serve",
    "start": "npm run start-docker && npm run build-app && npm run serve-app"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.17.11"
  }
}
